const appVue = new Vue({
    el: '#app',
    data: {
      productName: '',
      // Outros campos do produto
    },
    methods: {
      registerProduct() {
        // Lógica para enviar os dados do produto para o servidor
      }
    }
  });  